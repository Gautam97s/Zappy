import React from 'react'
import {
    ClerkLoading,
    SignedIn,
    SignedOut,
    SignInButton,
    UserButton,
} from '@clerk/nextjs'

type Props = {}

const ClerkAuthState = (props: Props) => {
  return (
    <>
    </>
  )
}

export default ClerkAuthState